# This starter kit does not require secrets for the default scripts.
# Keep real API keys, tokens, cookies, OAuth credentials, and private Sheet IDs out of public repos.

PROJECT_CONFIG_PATH=examples/sample-project.config.yaml
SAMPLE_LEADS_PATH=examples/sample-leads.csv
DRY_RUN_ONLY=true
